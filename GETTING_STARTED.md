# 🚀 راهنمای شروع پروژه

## مرحله 1: نصب Dependencies

```bash
npm install
```

این کامند تمام پکیجهای مورد نیاز را نصب میکند:
- React Three Fiber
- Three.js
- Zustand (state management)
- Tailwind CSS
- TypeScript

## مرحله 2: اجرای پروژه

```bash
npm run dev
```

پروژه روی `http://localhost:5173` اجرا میشود.

## مرحله 3: دانلود مدلهای 3D

### مدلهای ضروری:

1. **ماشین بازیکن** 🚗
   - برو به: https://sketchfab.com/search?q=low+poly+car
   - یک مدل Low Poly انتخاب کن
   - دانلود با فرمت `.glb`
   - ذخیره در: `src/assets/models/car.glb`

2. **درخت** 🌳
   - برو به: https://poly.pizza/search/tree
   - مدل Low Poly Tree
   - ذخیره در: `src/assets/models/tree.glb`

3. **کاکتوس** 🌵
   - برای محیط بیابان
   - ذخیره در: `src/assets/models/cactus.glb`

### سایتهای پیشنهادی:

- **Sketchfab**: https://sketchfab.com (نیاز به ثبتنام رایگان)
- **Poly Pizza**: https://poly.pizza (بدون ثبتنام)
- **Quaternius**: https://quaternius.com (مدلهای رایگان با کیفیت)
- **Kenny.nl**: https://kenney.nl/assets (Asset Pack های رایگان)

## مرحله 4: استفاده از مدلها

بعد از دانلود مدل ماشین:

1. فایل `src/scenes/GameScene.tsx` را باز کن
2. خط `<Player />` را با `<CarModel />` جایگزین کن
3. در `CarModel.tsx` کامنتها را برداری:

```tsx
const { scene } = useGLTF('/src/assets/models/car.glb');
// و
<primitive object={scene.clone()} scale={1} />
```

## مرحله 5: تست روی موبایل

برای تست روی موبایل:

```bash
npm run dev -- --host
```

سپس از IP نشان داده شده روی موبایلت استفاده کن.

## ساختار فایلها

```
src/
├── components/
│   ├── Game/          ← کامپوننتهای بازی
│   ├── UI/            ← رابط کاربری
│   └── Scene/         ← نورپردازی و محیط
├── hooks/             ← Custom hooks
├── store/             ← State management
├── utils/             ← توابع کمکی
├── assets/models/     ← مدلهای 3D را اینجا بذار
└── scenes/            ← صحنه اصلی بازی
```

## نکات مهم

✅ **بهینهسازی**:
- مدلهای Low Poly استفاده کن (< 10K polygons)
- تکسچرها را کوچک نگه دار (512x512 یا 1024x1024)
- از فرمت `.glb` استفاده کن (فشردهتر از `.gltf`)

✅ **کنترلها**:
- Desktop: Arrow Keys یا WASD
- Mobile: دکمههای لمسی خودکار نمایش داده میشوند

✅ **محیطها**:
- هر 30 ثانیه محیط تغییر میکند
- 4 محیط: جنگل، بیابان، برف، شب

## مشکلات رایج

### مدل نمایش داده نمیشود؟
- مسیر فایل را چک کن
- کنسول مرورگر را برای خطاها بررسی کن
- مطمئن شو فرمت `.glb` است

### عملکرد کند است؟
- تنظیمات کیفیت در `utils/performance.ts`
- Shadows را خاموش کن برای موبایل
- تعداد اشیاء را کم کن

### کنترلها کار نمیکنند؟
- مطمئن شو بازی Start شده (دکمه Start Journey)
- کنسول را برای خطاها چک کن

## مراحل بعدی

- [ ] اضافه کردن موانع
- [ ] سیستم امتیازدهی
- [ ] افکتهای صوتی
- [ ] موسیقی پسزمینه
- [ ] انیمیشنهای بیشتر
- [ ] پارتیکلها (باران، برف، گرد و غبار)

موفق باشی! 🎮✨
